generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_DATABASE_URL")
}

// Keep this schema aligned with prisma/schema.prisma for production Postgres deployments.
// For local preview and development, use sqlite schema.prisma.
